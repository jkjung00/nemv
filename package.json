{
  "name": "nemv",
  "version": "0.0.1",
  "scripts": {
    "dev": "SET NODE_ENV=development & node ./be/bin/www",
    "serve": "cd fe && npm run serve",
    "pro": "cd fe && npm run build && cd ../be && cd .. && SET NODE_ENV=production && SET PORT=80 && node ./be/bin/www",
    "pm2": "cd fe && npm run build && cd ../be && cd .. && pm2 start --env pro"
  },
  "dependencies": {
  }
}
